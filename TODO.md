# 系统画像页面顶部信息栏单行融合重构

请基于当前系统画像页面进行 UI 调整。

## 一、当前页面情况

当前搜索结果页面顶部有两行信息。

### 第一行

目前包含：

- 系统名称：s新核心\_数据&应用服务
- 系统等级：A+
- 切换
- 机房信息
- 简介

类似：

```text
s新核心_数据&应用服务  A+  切换   📍 主机房 北京永琪机房 合肥架构机房 北京东坝机房 ...   简介 提供以查询处理和文件处理等为核心的交易驱动型业务系统...
```

### 第二行

目前是四个统计卡片：

```text
11 组件类型
34 集群
120 主机
36 链路
```

---

# 二、本次核心要求

### 领导明确要求：

> **前两行必须合并成一个区域，不能再保留上下两行。**

因此本次改造的核心目标是：

> **把“系统基本信息 + 系统简介 + 四个统计指标”全部放进同一个横向信息栏中。**

最终页面顶部：

**只能有一行信息内容。**

不要通过缩小高度、减少 margin 等方式“假装合并”。

必须从布局结构上真正合并。

---

# 三、最终推荐布局

最终建议做成：

```text
┌───────────────────────────────────────────────────────────────────────────────────────────────┐
│ ● s新核心_数据&应用服务  A+  切换 │ 📍 合肥架构机房 北京永琪机房... │ 简介：提供以查询处理... │ 11 组件 │ 34 集群 │ 120 主机 │ 36 链路 │
└───────────────────────────────────────────────────────────────────────────────────────────────┘
```

也可以根据实际宽度调整成：

```text
┌───────────────────────────────────────────────────────────────────────────────────────────────┐
│ ● 系统名称  A+  切换 │ 📍 机房信息 │ 简介... │ 11组件 │ 34集群 │ 120主机 │ 36链路 │
└───────────────────────────────────────────────────────────────────────────────────────────────┘
```

核心不是完全照搬这个 ASCII，而是必须满足：

> **所有信息处于同一个横向区域，并且视觉上只有一行。**

---

# 四、第一部分：系统名称

系统名称必须保留为整个信息栏中视觉权重最高的内容。

例如：

```text
● s新核心_数据&应用服务
```

要求：

- 保留当前系统名称。
- 不要缩得过小。
- 不允许被统计数据挤到换行。
- 系统名称仍然可以点击。
- 点击系统名称可以进行系统切换。
- 保留当前已有的切换逻辑。

建议结构：

```text
● s新核心_数据&应用服务   A+   切换
```

其中：

- 系统名称：主要信息
- A+：弱化
- 切换：小型按钮/文字按钮

---

# 五、第二部分：机房

机房信息放在系统名称右侧。

例如：

```text
📍 合肥架构机房  北京永琪机房  合肥库房  北京南法信机房  深圳南湾机房...
```

要求：

1. 保留现有机房标签。
2. 机房整体不要占太大空间。
3. 如果机房过多，横向超出后显示省略号。
4. 不允许撑破整个顶部区域。
5. 不允许换成第二行。
6. 不影响右侧四个统计指标。

可以使用：

```css
overflow: hidden;
text-overflow: ellipsis;
white-space: nowrap;
```

或者项目现有的文本省略方案。

---

# 六、第三部分：简介

简介也必须放在同一行。

不要再把简介单独放到第二行。

例如：

```text
简介：提供以查询处理和文件处理等为核心的交易驱动型业务系统...
```

要求：

- 与机房处于同一个横向布局。
- 简介只允许单行。
- 超出宽度显示 `...`。
- 不允许换行。
- 不允许把顶部区域撑高。
- 不需要展示完整简介。
- hover 可以通过现有 Tooltip 查看完整内容。

推荐：

```css
overflow: hidden;
text-overflow: ellipsis;
white-space: nowrap;
```

---

# 七、第四部分：四个统计指标必须塞进同一行

这是本次改造最重要的地方。

当前：

```text
┌──────────────┐
│ 11 组件类型  │
└──────────────┘

┌──────────────┐
│ 34 集群      │
└──────────────┘

┌──────────────┐
│ 120 主机     │
└──────────────┘

┌──────────────┐
│ 36 链路      │
└──────────────┘
```

这些大卡片全部取消。

不要再使用现在这种四个宽卡片的布局。

改成：

```text
11 组件   34 集群   120 主机   36 链路
```

或者：

```text
▦ 11 组件   ▤ 34 集群   ▣ 120 主机   🔗 36 链路
```

---

# 八、指标文字必须缩短

为了保证整个信息栏能够一行放下，请主动缩短指标名称。

### 原来

```text
11 组件类型
34 集群
120 主机
36 链路
```

### 改成

```text
11 组件
34 集群
120 主机
36 链路
```

其中：

```text
组件类型 → 组件
```

这是必须修改的。

如果实际宽度仍然不够，可以进一步使用：

```text
11 组件
34 集群
120 主机
36 链路
```

不要使用：

```text
11 组件类型
34 集群数量
120 主机数量
36 链路数量
```

禁止添加“数量”等冗余文字。

---

# 九、统计指标的视觉设计

统计指标不再是独立的大卡片。

应该变成：

```text
11 组件   │   34 集群   │   120 主机   │   36 链路
```

或者使用非常轻量的分隔方式。

数字：

- 稍微突出
- 保留当前不同指标的颜色体系/图标体系

文字：

- 比数字弱
- 字号略小

例如：

```text
11 组件
```

而不是：

```text
11
组件类型
```

也就是说：

> **从“卡片式统计”改成“行内紧凑统计”。**

---

# 十、推荐的完整布局结构

请优先实现下面这种布局：

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│ ● s新核心_数据&应用服务  A+  切换 │ 📍 合肥架构机房 北京永琪机房... │ 简介：提供以查询处理... │ 11 组件 │ 34 集群 │ 120 主机 │ 36 链路 │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

可以把整个区域拆成 4 个逻辑部分：

```text
[系统信息] [机房] [简介] [统计指标]
```

即：

```text
SystemInfo | Rooms | Description | Metrics
```

---

# 十一、宽度分配建议

不要给四个统计指标固定很大的宽度。

建议采用：

### 系统信息

```text
flex: 0 0 auto
```

但需要设置最大宽度。

---

### 机房

可以：

```text
flex: 0 1 auto;
```

并设置最大宽度。

---

### 简介

简介应该是最主要的可压缩区域：

```text
flex: 1 1 auto;
min-width: 100px;
```

这样当页面空间不足时：

> 优先压缩简介，而不是压缩系统名称和统计数据。

---

### 统计指标

四个指标：

```text
flex: 0 0 auto;
```

保持完整显示。

---

# 十二、空间不足时的优先级

这是非常重要的。

当浏览器宽度不足时，内容应该按照以下优先级处理：

### 第一优先级

系统名称

必须保留。

### 第二优先级

四个统计指标

必须保留。

### 第三优先级

机房

可以逐渐减少可见内容并省略。

### 第四优先级

简介

优先省略。

也就是说：

```text
空间足够：

系统名称 | 机房 | 简介 | 统计

空间不足：

系统名称 | 机房... | 简介... | 统计

空间非常不足：

系统名称 | 机房... | 简介... | 11组件 34集群 120主机 36链路
```

但：

> **绝对不允许统计指标换到第二行。**

---

# 十三、不要使用四个大卡片

当前：

```text
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ 11 组件类型 │ │ 34 集群      │ │ 120 主机     │ │ 36 链路      │
└─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘
```

本次必须取消这种结构。

改成：

```text
11 组件 │ 34 集群 │ 120 主机 │ 36 链路
```

目的就是：

> **最大程度减少顶部区域占用空间。**

---

# 十四、整体视觉要求

虽然压缩成一行，但是不能让页面看起来像一串挤在一起的文字。

需要保留合理的视觉分组。

推荐：

```text
● 系统名称  A+  切换
        │
📍 机房
        │
简介...
        │
11 组件   34 集群   120 主机   36 链路
```

通过：

- margin
- gap
- 分隔线
- 字体大小
- 字体颜色
- 图标

形成清晰的信息层级。

不要增加新的大卡片。

---

# 十五、统计指标的推荐视觉形式

优先尝试：

```text
▦ 11 组件    ▤ 34 集群    ▣ 120 主机    🔗 36 链路
```

如果当前项目已经有对应 icon，则继续使用现有 icon。

不要为了这次改造新增 icon 库。

如果现有图标在一行布局中显得太占空间，可以适当缩小。

---

# 十六、系统名称切换功能必须保留

系统名称现在已经有：

```text
切换
```

按钮。

本次不要删除这个能力。

可以优化为：

```text
● s新核心_数据&应用服务   A+   [切换]
```

或者：

```text
● s新核心_数据&应用服务   A+   ↗
```

具体视觉根据当前项目组件风格决定。

但是功能必须保持：

```text
点击切换
      ↓
重新选择系统
      ↓
重新搜索
      ↓
更新当前系统画像
```

不要修改现有查询 API。

---

# 十七、简介和机房的处理原则

特别注意：

不要因为领导要求“一行”而删除简介。

简介仍然需要存在。

只是：

> **简介从原来的独立视觉信息变成顶部横向信息栏中的一个压缩字段。**

最终：

```text
系统名称 | 机房 | 简介... | 11组件 | 34集群 | 120主机 | 36链路
```

而不是：

```text
第一行：系统名称 | 机房 | 简介

第二行：11组件 | 34集群 | 120主机 | 36链路
```

后者本次禁止。

---

# 十八、默认状态不要修改

本次主要修改：

> **搜索结果状态顶部信息区域。**

默认状态之前已经调整好的居中搜索框保持不变。

除非代码结构导致必须调整，否则：

- 不要修改默认状态
- 不要修改搜索框
- 不要修改空状态
- 不要修改页面其他区域

---

# 十九、不要影响下面的业务区域

以下内容保持现状：

```text
组件拓扑 / 组件类型
        ↓
主机列表
        ↓
链路关系图
```

不要因为顶部高度调整而修改这些区域的业务逻辑。

顶部变矮以后，下面内容自然获得更多可视空间，这是本次改造希望达到的效果。

---

# 二十、实施前先分析代码

不要直接修改。

首先找到：

1. 系统画像页面
2. 顶部系统信息组件
3. 统计指标组件
4. 机房组件
5. 简介展示组件
6. 系统切换逻辑
7. 当前 CSS / SCSS / Tailwind
8. 当前响应式布局

然后判断：

> 如何在最小改动下把两个区域合并成一个横向布局。

不要重构整个页面。

---

# 二十一、实现要求

必须遵循当前项目：

- 技术栈
- UI 组件库
- CSS 规范
- Design Token
- TypeScript 规范
- ESLint
- Prettier
- 组件命名规范

不要：

- 引入新的 UI 框架
- 引入新的依赖
- 重写 API
- 重写数据结构
- 重写状态管理
- 重构无关代码

---

# 二十二、最终验收标准

## 顶部布局

- [ ] 第一行和第二行已经真正合并
- [ ] 搜索结果状态顶部只有一个信息栏
- [ ] 不允许出现第二行统计卡片
- [ ] 四个指标全部位于同一行
- [ ] 顶部区域高度明显降低

## 系统信息

- [ ] 系统名称正常
- [ ] 系统等级正常
- [ ] 切换功能正常
- [ ] 机房正常
- [ ] 简介正常
- [ ] 简介超长显示省略号
- [ ] 不允许简介换行

## 统计信息

- [ ] `组件类型` 缩短成 `组件`
- [ ] `11 组件` 正常显示
- [ ] `34 集群` 正常显示
- [ ] `120 主机` 正常显示
- [ ] `36 链路` 正常显示
- [ ] 不再使用四个大统计卡片
- [ ] 四个统计指标横向排列
- [ ] 指标之间有合理间距
- [ ] 数字比文字更突出

## 页面整体

- [ ] 页面顶部明显更加紧凑
- [ ] 不产生横向滚动
- [ ] 不产生第二行
- [ ] 不出现文字重叠
- [ ] 不影响组件类型区域
- [ ] 不影响主机列表
- [ ] 不影响链路关系图
- [ ] 不影响原有搜索逻辑
- [ ] 不影响系统切换
- [ ] 不影响路由和 Tab

---

# 二十三、最终目标

请把当前：

```text
第一行：
系统名称 + A+ + 切换 + 机房 + 简介

第二行：
11 组件类型 | 34 集群 | 120 主机 | 36 链路
```

彻底优化成：

```text
● 系统名称  A+  切换 │ 📍 机房... │ 简介... │ 11 组件 │ 34 集群 │ 120 主机 │ 36 链路
```

最终要求只有一个核心：

> **系统基本信息、机房、简介、四个统计指标，全部在同一个横向信息栏里，一行展示。**

不要为了实现“一行”简单粗暴地缩小所有字体。

应该通过：

1. 缩短指标名称
2. 取消四个大统计卡片
3. 将统计变成行内紧凑指标
4. 压缩机房区域
5. 简介单行省略
6. 合理使用 flex
7. 调整 gap / padding
8. 保留系统名称视觉重点

来完成真正的布局优化。

完成后执行项目现有的 typecheck、lint、build，并检查最终页面视觉效果。
