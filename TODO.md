请重新设计当前“告警分析”页面中的「更多字段」弹窗。

【非常重要】

这次只修改「更多字段」弹窗的 UI 展示和样式。

不要修改：

- 后端接口
- 数据结构
- 字段名称
- 字段值
- 数据处理逻辑
- “更多字段”的打开方式
- P2/P3 页面
- P1 指标区域
- 表格其他功能

现有所有字段必须继续动态展示。

目标是：

把现在这个“像开发调试页面的字段列表”，改成一个真正的「告警详情」弹窗。

========================
一、当前问题
========================

当前弹窗主要问题：

1. 整体视觉比较粗糙；
2. 两列字段直接铺开，像 JSON 属性面板；
3. label 和 value 的视觉层级不够明显；
4. 字段之间大量浅灰色背景块，导致页面很碎；
5. 顶部 alertKey 区域设计比较突兀；
6. 字段名和字段值之间缺乏清晰的信息层级；
7. 时间戳和格式化时间同时展示，视觉比较乱；
8. metric、summary 等超长文本展示非常拥挤；
9. 弹窗整体像“查看对象属性”，不像“查看告警详情”；
10. 弹窗内部缺少合理的分组和重点信息。

========================
二、整体设计目标
========================

设计成：

「现代企业级后台 + 告警详情」

风格关键词：

- 简洁
- 专业
- 紧凑
- 信息密度高
- 清晰
- 现代
- 数据型后台

不要：

- 大面积渐变
- 玻璃拟态
- 炫酷动画
- 大圆角
- 大阴影
- 彩色装饰
- 过度设计

整体要和当前 Ruoyi Vue2 管理后台保持一致。

========================
三、弹窗尺寸
========================

当前弹窗尺寸和比例不够舒服。

建议：

宽度：

760px ~ 860px

高度：

根据内容自适应。

不要做成特别高的弹窗。

如果字段很多：

弹窗固定最大高度，例如：

70vh ~ 75vh

内部滚动。

不要让整个浏览器页面跟着滚动。

========================
四、顶部 Header 重新设计
========================

当前顶部：

“更多字段”

下面直接放 alertKey。

请改成真正的告警详情 Header。

例如：

┌────────────────────────────────────────────┐
│ 告警详情 × │
│ ALERT / 20260913-117 │
└────────────────────────────────────────────┘

标题：

告警详情

下面显示：

alertKey

但不要让 alertKey 占据特别大的空间。

alertKey 可以使用：

较小字号
等宽字体 / 半等宽字体
较弱颜色

如果 alertKey 很长：

允许截断 + tooltip 查看完整内容。

========================
五、增加告警摘要区域
========================

顶部 Header 下面增加一个非常轻量的 Summary 区域。

重点展示：

- alertKey
- summary
- level
- status
- sourceName

例如：

告警级别 2
告警状态 2
来源 应用层
告警摘要 系统授权交易\_关键交易码PAYBNK...

其中：

summary 是最重要的描述信息。

应该允许它占比较大的宽度。

summary 不要和普通字段一样处理。

可以设计成：

┌────────────────────────────────────────────┐
│ 告警摘要 │
│ 系统授权交易\_关键交易码PAYBNK,指标... │
└────────────────────────────────────────────┘

但整体仍然要紧凑。

========================
六、字段布局
========================

剩余字段继续使用两列布局。

不要改成单列。

例如：

┌────────────────────┬────────────────────┐
│ actual_value │ createTime │
│ 0.0 │ 2024-10-07 05:00 │
├────────────────────┼────────────────────┤
│ endTime │ gte │
│ 2026-09-13 05:26 │ 1729248102803 │
├────────────────────┼────────────────────┤
│ idc │ level │
│ HZ-ZB │ 2 │
├────────────────────┼────────────────────┤
│ lte │ metric │
│ 1729248402851 │ 网联银联交易成功率... │
└────────────────────┴────────────────────┘

但是不要使用当前这种明显的灰色“输入框”效果。

字段应该更像：

「Label + Value」

而不是：

「Form Input」。

========================
七、字段样式
========================

推荐：

字段名：

font-size: 12px
color: #86909C

字段值：

font-size: 13px
color: #1D2129

字段名和值上下排列。

例如：

actual_value
0.0

createTime
2024-10-07 05:00:02

这样会比：

actual_value | 0.0

更加清晰。

但是要注意：

不要增加太多高度。

每个字段控制在：

48~56px

左右。

========================
八、不要给每个字段都做明显 Card
========================

这是非常重要的。

不要：

┌────────────┐
│ actual_value│
│ 0.0 │
└────────────┘

┌────────────┐
│ createTime │
│ 2024... │
└────────────┘

这种会非常碎。

推荐：

使用非常浅的背景 + 极淡分割线。

或者：

只有 hover 时出现背景。

整体应该是一个连续的信息区域。

========================
九、长文本字段重点优化
========================

以下字段可能非常长：

- summary
- metric
- metric_link

不要让它们把整个布局撑坏。

处理方式：

summary：

允许 1~2 行。

metric：

允许 2 行。

metric_link：

单行截断 + tooltip 查看完整内容。

例如：

metric_link
https://pm.eprod-kz.../config/reportdata?currentTab=...

hover 后显示完整内容。

不要出现严重的文字挤压。

========================
十、时间字段优化
========================

当前很多时间字段同时出现：

17282844802855
2024-10-07 05:00:02

导致视觉非常混乱。

请优化时间展示。

如果当前数据同时包含：

时间戳 + 格式化时间

优先展示：

2024-10-07 05:00:02

时间戳可以作为 tooltip 或 secondary 信息。

例如：

createTime
2024-10-07 05:00:02

hover：

timestamp: 17282844802855

不要让两个时间值抢占视觉空间。

注意：

不要修改真实数据，
只是优化展示方式。

========================
十一、字段分组
========================

如果当前字段较多，可以进行轻量分组。

推荐：

【基础信息】

alertKey
summary
level
status
sourceName

【告警时间】

createTime
startTime
endTime
receiveTime

【告警指标】

metric
actual_value
gte
lte
metric_link

【系统信息】

idc
runId
systemId

注意：

分组标题必须非常轻量。

例如：

基础信息

不要设计成巨大的 Card Header。

分组之间保持较小间距。

如果实际字段不适合以上分组，
根据当前字段语义合理归类。

如果分组会导致弹窗高度明显增加，
宁可不分组。

========================
十二、重要字段突出
========================

重点突出：

summary
level
status
actual_value
metric

其中：

level / status

可以使用非常轻量的 Tag。

例如：

level
[ 2 ]

status
[ 2 ]

不要使用鲜艳的大红、大绿。

只做轻量状态标识。

========================
十三、底部操作区
========================

如果当前没有实际操作按钮：

不要强行增加 Footer。

只保留关闭按钮。

如果当前已有关闭逻辑：

保持不变。

========================
十四、滚动行为
========================

弹窗内容超过最大高度时：

只有弹窗 body 内部滚动。

Header 固定。

例如：

┌─────────────────────────────┐
│ 告警详情 × │ ← 固定
├─────────────────────────────┤
│ │
│ 内容 │
│ 内容 │
│ 内容 │
│ ↓ 内部滚动 │
│ │
└─────────────────────────────┘

不要整个弹窗随着页面滚动。

========================
十五、视觉细节
========================

推荐：

弹窗背景：

#FFFFFF

边框：

#E5E6EB

标题：

#1D2129

辅助文字：

#86909C

主色：

沿用系统当前蓝色。

圆角：

6~8px

阴影：

轻微。

不要过度圆角。

不要使用渐变。

========================
十六、交互细节
========================

支持：

1. 长文本 tooltip；
2. hover 字段轻微背景变化；
3. 内容复制（如果项目已有通用复制能力，可以复用）；
4. 弹窗内部滚动。

不要新增复杂动画。

不要新增复杂交互。

========================
十七、非常重要：保持数据动态
========================

所有字段必须继续来自当前接口数据。

不要写死：

alertKey
summary
level
status
actual_value
createTime
等等。

只改变：

「如何展示」

不要改变：

「数据来源和业务逻辑」。

========================
十八、技术限制
========================

这是现有 Ruoyi Vue2 项目。

请遵循当前项目技术栈。

不要：

- 升级 Vue
- 更换 Element UI
- 引入新的 UI 框架
- 修改后端接口
- 修改 API
- 修改数据结构
- 修改其他页面

优先复用项目已有：

- Dialog
- Tooltip
- Tag
- Icon
- CSS
- 日期格式化工具

========================
十九、最终效果要求
========================

最终不要再有现在这种：

“一个白色弹窗 + 一堆灰色小方框 + 两列 JSON 字段”

而应该像：

「企业级监控系统里的告警详情面板」。

视觉层级：

告警详情
↓
告警摘要
↓
基础信息 / 时间 / 指标 / 系统信息
↓
普通字段

信息密度保持高。

弹窗不要明显变高。

整体要比当前版本：

更简洁
更现代
更专业
更容易扫描。

========================
二十、实施
========================

请先找到当前“更多字段”弹窗对应的 Vue 组件。

阅读现有：

- template
- data
- computed
- methods
- dialog 配置
- 字段渲染逻辑
- CSS

然后直接修改代码。

不要只给设计方案。

完成后自行检查：

1. 弹窗是否仍然正常打开；
2. 所有字段是否正常显示；
3. 长文本是否正常；
4. 时间是否正常；
5. 两列布局是否正常；
6. 内部滚动是否正常；
7. 关闭按钮是否正常；
8. P2/P3 是否没有受到影响；
9. 没有修改接口和业务逻辑。

最终目标：

「这是一个告警详情弹窗，而不是后端对象字段查看器。」
