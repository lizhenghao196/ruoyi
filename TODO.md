请根据以下要求和数据，重新设计一个后台「系统画像」页面。技术栈为 Vue2 + Element UI（RuoYi 风格），目标分辨率 1920×1080，要求整页尽量不滚动，核心信息一屏展示。
一、设计目标

提升页面质感和设计感，避免原来扁平、工具感过强的风格。
整页高度控制在一屏内（约 900px 可用高度），只有主机列表区域允许内部滚动。
默认选中第一个组件，左侧主机列表只显示该组件下的主机。
链路功能暂不实现，但必须预留固定高度的区域，后续用 ECharts 做链路关系图。
保持专业后台气质，现代、干净、有层次。

二、推荐布局结构（严格按此结构实现）
text┌────────────────────────────────────────────────────────────────────┐
│ 顶部基础信息栏（高度约 56px） │
│ 系统名 + A+ 重要性标签 + 主机房 + 数据中心标签（可横向滚动）+ 简介（1行省略，悬停显示全部） │
├──────────┬──────────┬──────────┬───────────────────────────────────┤
│ 11 组件类型 │ 34 集群 │ 41 主机 │ 36 链路（预留，显示数字即可） │ ← 统计卡片行（高度约 72px）
├──────────┴──────────┴──────────┴───────────────────────────────────┤
│ 组件拓扑区域（高度约 90px） │
│ 11个组件以紧凑标签/小卡片形式展示，带数量角标，支持点击筛选，默认选中第一个 │
├────────────────────────────────┬───────────────────────────────────┤
│ 左侧：主机列表（宽度约 45%） │ 右侧：链路关系图预留区（宽度约 55%） │
│ - 按集群分组展示 │ - 固定高度 420~480px │
│ - 表头固定，内容区域内部滚动 │ - 先放精致占位卡片（虚线边框+图标+文字） │
│ - 只显示当前选中组件的主机 │ - 后续直接替换为 ECharts 图表 │
│ - 支持主机名/IP 搜索 │ │
└────────────────────────────────┴───────────────────────────────────┘
三、视觉与质感要求

页面背景使用极浅灰蓝色（推荐 #f5f7fa 或 #f8fafc），不要纯白。
所有卡片增加轻微阴影（box-shadow: 0 2px 12px rgba(0,0,0,0.06)）和 8~12px 圆角。
统计卡片数字要大且突出，标签字号较小。
组件标签使用不同淡色背景区分类型（如 Nginx 蓝、Kafka 橙、MySQL 绿、ES 紫等），带圆角和轻微边框。
字体层级清晰：标题、数字、正文、次要信息要有明显对比。
主机列表表头使用浅色背景，行 hover 有淡色反馈，类型/机房等字段用带背景的标签展示。
整体间距舒适，有呼吸感，但信息密度不能过低。

四、交互要求

组件拓扑支持点击，点击后：
该组件高亮
左侧主机列表只显示该组件下的主机（按集群分组）

默认选中第一个组件（按数据顺序）。
主机列表支持搜索（主机名或 IP）。
系统简介超过一行时省略，鼠标悬停显示完整内容。

五、数据说明（请使用以下真实数据结构）
系统基础信息、组件拓扑、主机详情、集群关系、链路数据均已提供（见下方完整 JSON）。请根据真实数据计算统计数字，并正确渲染主机列表。
（数据在mockData里面的res.js中的systemProfileRes）
六、输出要求
请直接输出完整的 Vue2 单文件组件代码（.vue），包含：

template
script（包含模拟数据或接收 props）
style（scoped，体现质感）

代码需可直接运行，样式精致，布局符合上述结构。链路区域先做好占位即可
