# HDP容量报表 vxe-table 表格样式现代化优化任务

你是一名拥有丰富经验的高级前端工程师，同时具备企业级后台管理系统 UI 设计能力。

现在需要对一个 Vue2 + 若依（RuoYi）项目中的 HDP容量报表进行 UI 优化。

本次任务的范围非常明确：

# 只优化现有 vxe-table 表格的视觉样式。

⚠️ 不允许新增任何页面组件。
⚠️ 不允许改变现有页面整体布局。
⚠️ 不允许修改业务逻辑。
⚠️ 不允许修改接口。
⚠️ 不允许修改数据结构。
⚠️ 不允许删除现有字段。
⚠️ 不允许新增 Dashboard、统计卡片、图表等内容。

本次只允许通过：

- vxe-table 配置
- 表格 CSS / SCSS
- 表头样式
- 单元格样式
- 行样式
- 边框
- 字体
- 颜色
- hover 效果
- 斑马纹
- 单元格状态样式
- 数字对齐方式
- 多级表头视觉层级

对现有表格进行视觉优化。

---

# 一、项目技术环境

当前项目：

- Vue2
- 若依（RuoYi）
- Element UI
- vxe-table
- SCSS

要求：

- 不升级 Vue3
- 不替换 vxe-table
- 不引入新的 UI 框架
- 不增加新的第三方依赖
- 优先复用现有代码
- 使用现有项目的技术体系完成优化

---

# 二、当前页面情况

当前页面为：

「HDP容量报表」

表格主要包含以下类型的数据：

## 基础信息

- 集群名称
- PATH
- 空间占用（TB）

## 增长率数据

- 日增长率（%）
- 周增长率（%）
- 月增长率（%）
- 年增长率（%）

每个增长率下面可能存在多级表头，例如：

- 最近7天折算
- 最近7天折算
- 最近30天折算
- 最近90天折算

## 当前状态

- 当前使用率
- 多维预警空间使用
- 备注

## 其他信息

- 目录清理策略
- 目录清理规范

当前页面已经具备完整功能。

不要修改现有功能。

---

# 三、本次优化目标

当前表格的问题主要是：

1. 整体边框过多
2. 单元格格子感很强
3. 看起来比较像传统 Excel
4. 表头层级不够明显
5. 所有数据视觉权重差不多
6. 数据密集但缺少阅读节奏
7. hover 效果不明显
8. 多级表头没有体现出清晰层级
9. 当前使用率等重要数据不够突出
10. 风险数据目前采用整块单元格红色背景，视觉比较传统
11. 数字列对齐方式不够统一
12. 整体缺少现代企业级数据平台的精致感

本次优化目标：

将当前表格从：

「传统后台管理系统 Excel 风格」

升级为：

「现代企业级运维数据管理平台风格」

整体关键词：

- 简洁
- 专业
- 克制
- 现代
- 高信息密度
- 易读
- 数据优先
- 不花哨

---

# 四、非常重要的设计原则

本次优化不要过度设计。

禁止：

❌ 新增统计卡片
❌ 新增图表
❌ 新增 Dashboard
❌ 大面积渐变
❌ 大面积阴影
❌ 大量圆角
❌ 五颜六色
❌ 每个数据都使用 Tag
❌ 每个单元格增加背景
❌ 复杂动画
❌ 修改页面布局
❌ 修改表格字段
❌ 修改业务逻辑

最终效果应该是：

「第一眼看到的是数据，而不是格子。」

---

# 五、表格整体边框优化

当前表格最大的问题之一是：

纵向和横向边框都比较明显，导致整个表格看起来像 Excel。

请重点优化边框。

## 优化要求

### 1、弱化纵向边框

不要让每一个单元格都有明显的竖线。

纵向边框使用非常浅的颜色，例如：

#f0f2f5

或者进一步弱化。

目标：

降低格子感。

---

### 2、保留轻微横向分割

每一行之间保留浅色分割线：

border-bottom: 1px solid #ebeef5;

颜色必须轻。

不要出现明显的横线。

---

### 3、表格外层边框

表格整体可以保留一个非常轻微的外边框：

border: 1px solid #ebeef5;

border-radius: 4px;

注意：

圆角不要太大。

不要添加明显的 box-shadow。

---

# 六、表头样式优化

表头需要成为整个表格的视觉层级中心。

当前表头比较普通。

请优化。

## 普通表头

背景颜色：

#f7f9fc

文字颜色：

#303133

字体：

font-size: 13px;

font-weight: 600;

高度：

44px ~ 48px

建议：

letter-spacing: 0.2px;

表头文字保持清晰。

---

## 表头底部分割

表头和数据区域之间增加：

border-bottom: 1px solid #dcdfe6;

但不要太明显。

目的是区分：

表头区域

和

数据区域。

---

# 七、多级表头优化

当前表格存在多级表头。

例如：

日增长率（%）
└ 最近7天折算

周增长率（%）
└ 最近7天折算

月增长率（%）
└ 最近30天折算

年增长率（%）
└ 最近90天折算

请重点体现多级表头层级。

---

## 一级分组表头

例如：

日增长率（%）
周增长率（%）
月增长率（%）
年增长率（%）

样式：

background: #f3f6fa;

font-weight: 600;

color: #303133;

font-size: 13px;

---

## 二级表头

例如：

最近7天折算
最近30天折算
最近90天折算

样式：

background: #fafbfc;

font-size: 12px;

font-weight: 400;

color: #606266;

---

要求：

通过：

- 背景色深浅
- 字体大小
- 字体权重

体现层级。

不要使用粗边框。

不要使用明显色块。

整体保持克制。

---

# 八、表格行样式优化

普通行：

background: #ffffff;

行高：

44px ~ 48px。

请根据当前表格实际情况调整。

不要让数据显得过于拥挤。

---

## 斑马纹

建议增加非常轻微的斑马纹。

例如：

偶数行：

#ffffff

奇数行：

#fafcff

注意：

颜色差异必须非常轻。

用户不应该明显感觉：

「这一行是灰色。」

而应该只是自然帮助阅读。

---

# 九、hover 效果优化

当前鼠标经过表格行时反馈不够明显。

请增加现代化 hover。

要求：

鼠标经过整行：

background-color: #f3f8ff;

增加：

transition: background-color 0.2s ease;

注意：

不要使用深蓝色。

不要使用明显高亮。

目标：

让用户知道当前鼠标所在行。

但不要破坏整体视觉。

---

# 十、字体层级优化

当前所有数据字体基本一样。

请建立信息层级。

---

## 一级信息

例如：

- 集群名称
- 空间占用
- 当前使用率

样式：

color: #303133;

font-weight: 500;

---

## 普通数据

例如：

增长率等普通数据。

样式：

color: #606266;

font-weight: 400;

---

## 辅助信息

例如：

PATH
备注
目录清理规范

样式：

color: #909399;

font-size: 12px;

---

要求：

不要所有文字都是黑色。

通过不同灰度建立阅读层级。

---

# 十一、集群名称列优化

集群名称属于核心识别信息。

例如：

集群1-Total
集群2-Total
集群3
集群4

请使用：

font-weight: 500;

color: #303133;

不要增加复杂图标。

不要增加 Tag。

只通过：

字体颜色
字体权重

进行强调。

---

## Total 类型数据

例如：

集群1-Total

可以适当增加视觉权重。

建议：

font-weight: 600;

颜色略深。

但不要使用：

- 大面积背景
- Tag
- Badge

保持专业。

---

# 十二、PATH 列优化

PATH 属于技术信息。

例如：

/apps/hive/warehouse/business_dw.db

/data/kafka/topics/pay_callback

/apps/hive/warehouse/logs_ods.db

请使用等宽字体：

font-family:
Consolas,
Monaco,
Menlo,
monospace;

字体大小：

12px;

颜色：

#606266;

---

如果路径过长：

white-space: nowrap;

overflow: hidden;

text-overflow: ellipsis;

鼠标 hover 时保留现有 Tooltip 或使用 title 显示完整路径。

不要给 PATH 添加明显的背景色。

保持低调。

---

# 十三、数字列优化

对于：

空间占用（TB）

以及其他数字类型字段。

请统一使用：

font-variant-numeric: tabular-nums;

数字使用右对齐。

例如：

3204.76
128.22
18.88
3415.47

右对齐能够让不同数据更加容易比较。

这是数据表格的重要优化。

---

## 数字字体

建议：

font-size: 12px ~ 13px;

color: #4e5969;

不要过粗。

---

# 十四、增长率列优化

当前增长率：

+0.3%
-1.5%
+125.3%
-22.6%

全部使用相同样式。

请增加轻微的数据趋势区分。

---

## 正增长

例如：

+0.3%
+5.4%
+125.3%

使用：

color: #e6a23c;

注意：

使用低饱和度橙色。

不要使用非常鲜艳的红色。

---

## 负增长

例如：

-1.5%
-4.0%
-22.6%

使用：

color: #67c23a;

使用柔和绿色。

---

## 接近0

例如：

0%
0.1%

使用：

color: #909399;

---

注意：

不要添加复杂图标。

不要新增额外 DOM。

优先通过：

cell-class-name

或者 slot class

实现。

---

# 十五、当前使用率重点优化

「当前使用率」是当前表格最重要的数据之一。

例如：

66%
76%
24.5%
85.1%
65.9%

目前存在的问题：

超过阈值的数据直接整块单元格变红。

这种方式比较传统。

请取消：

整个 td 大面积变红。

---

## 优化方式

只突出百分比数字。

根据不同使用率增加不同 class。

例如：

### 正常

0% ~ 60%

class：

usage-normal

样式：

color: #409eff;

font-weight: 600;

---

### 关注

60% ~ 70%

class：

usage-focus

样式：

color: #409eff;

font-weight: 600;

---

### 预警

70% ~ 85%

class：

usage-warning

样式：

color: #e6a23c;

font-weight: 600;

background:

rgba(230, 162, 60, 0.08);

---

### 高风险

85% ~ 100%

class：

usage-danger

样式：

color: #f56c6c;

font-weight: 600;

background:

rgba(245, 108, 108, 0.08);

---

注意：

背景只包裹数字内容。

不要让整个 td 变色。

建议效果：

[ 76% ]

[ 85.1% ]

类似一个非常轻量的状态标签。

但不要使用 Element Tag 组件。

直接使用 span + CSS 实现。

例如：

<span class="usage-value usage-warning">
  76%
</span>

样式：

padding: 3px 8px;

border-radius: 4px;

font-weight: 600;

注意：

视觉必须克制。

不要做成明显的标签。

---

# 十六、备注列优化

备注属于辅助信息。

例如：

距离70%的阈值，还剩XX天

建议清理XX数据

样式：

font-size: 12px;

color: #909399;

line-height: 1.5;

不要加粗。

---

如果是风险备注：

可以使用：

color: #e6a23c;

进行轻微强调。

如果是高风险：

color: #f56c6c;

但是不要：

整块背景变色。

---

# 十七、不同数据区域的视觉区分

当前表格列非常多。

可以通过极其轻微的背景色帮助用户区分不同类型数据。

例如：

## 基础信息

集群名称
PATH
空间占用

正常白色背景。

---

## 增长趋势数据

日增长率
周增长率
月增长率
年增长率

可以使用：

#fcfdff

或者：

#fafcff

注意：

必须非常浅。

用户不能明显感觉：

「这里有一个蓝色区域。」

而只是自然产生视觉分组。

---

## 当前状态

当前使用率

保持白色。

通过数字状态突出。

---

注意：

不要对每一列增加明显背景。

不要形成五颜六色的表格。

---

# 十八、排序图标优化

当前表格存在排序功能。

请优化 vxe-table 排序图标。

默认状态：

color: #c0c4cc;

hover：

color: #409eff;

active：

color: #409eff;

排序图标尺寸保持小。

不要比表头文字更突出。

---

# 十九、固定列样式优化

如果当前表格存在固定列。

固定列和滚动区域之间不要使用明显的粗边框。

可以增加非常轻微的阴影：

box-shadow: 4px 0 8px rgba(0, 0, 0, 0.03);

或者：

border-right: 1px solid #f0f2f5;

目标：

让固定区域自然过渡。

不要产生明显的分割线。

---

# 二十、表格间距优化

请优化：

单元格 padding。

推荐：

padding-left: 12px;

padding-right: 12px;

不要：

padding 过大。

因为这是数据密集型表格。

目标：

既不拥挤，也不要浪费空间。

---

# 二十一、颜色规范

整个表格统一使用以下颜色体系。

页面主要文字：

#303133

普通文字：

#606266

辅助文字：

#909399

表头背景：

#f7f9fc

一级分组表头：

#f3f6fa

斑马纹：

#fafcff

hover：

#f3f8ff

边框：

#ebeef5

排序激活：

#409eff

增长：

#e6a23c

下降：

#67c23a

预警：

#e6a23c

危险：

#f56c6c

注意：

颜色只用于表达信息状态。

不要让整个表格变成彩色。

---

# 二十二、推荐 vxe-table 样式实现方式

请优先使用 vxe-table 自带能力。

例如：

- header-cell-class-name
- cell-class-name
- row-class-name
- row-style
- cell-style
- header-cell-style

根据不同列和数据返回不同 class。

例如：

```javascript
cellClassName({ row, column }) {
  if (column.field === 'usageRate') {
    if (row.usageRate >= 85) {
      return 'usage-danger-cell'
    }

    if (row.usageRate >= 70) {
      return 'usage-warning-cell'
    }
  }

  if (column.field === 'dayGrowth') {
    return row.dayGrowth > 0
      ? 'growth-positive'
      : 'growth-negative'
  }
}
```
