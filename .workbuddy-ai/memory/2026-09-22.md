# 2026-09-22

## 工单甘特图：柱体文字从白字换成同色系深色

**用户反馈**：「手动绿条上的文字看不清，文字是白色的；自动蓝条上也看不清」。

**定位（先量再改）**：柱体底色是「浅 → 中」的渐变（`--og-*-a` → `--og-*-b`），白字对比度低到不可读：

| 柱体 | 白字对比度 | 改后（深色字） |
| --- | --- | --- |
| 手动绿 `#a7f3d0 → #34d399` | 1.44 → 1.92 | `#065f46`：5.99 → 4.00 |
| 自动蓝 `#bfdbfe → #5b9cf8` | 1.58 → 2.32 | `#1e3a8a`：7.29 → 3.73 |

（`og_ink_check.mjs` 按 5% 步长沿渐变扫全区间算的 WCAG 对比度。）

**改动（只动 `components/OrderGantt.vue` 的 style 块，JS 一行没碰）**：
1. `.og` 里加 `--og-manual-ink: #065f46` / `--og-auto-ink: #1e3a8a`（跟已有的 `--og-*-a/b/soft` 同族命名）。
2. `.og-bar.is-manual/.is-auto` 各映射 `--og-bar-text: var(--og-*-ink)`；`.og-bar` 基类留 `--og-bar-text: var(--og-text-2)` 兜底。
3. `.og-bar__text` 的 `color` 从 `rgba(255,255,255,0.96)` → `var(--og-bar-text)`，**删掉 text-shadow**（那是给白字救场的，深字挂着会把边缘糊脏）。

**为什么选这两个色**：都取同色系 800/900 档，WCAG 相对亮度 0.087 / 0.051（差 0.035），
两组并排不会一边重一边轻；最差位置对比度 4.00 / 3.73，接近。

**踩到的坑**：
- 「两组文字明度接近」不能用 HSL 明度 `(max+min)/2` 判 —— 蓝色系 B 通道天生把它抬高，
  深蓝会被误判成比深绿轻（实测差 0.13 假失败）。改用 **WCAG 相对亮度**，差 0.035 通过。
- `og_scss.mjs` 里检测 `text-shadow` 要先剥注释：新写的注释里就含「text-shadow」这几个字，否则误报。

**顺手修掉的既有问题（与本次改动无关，纯属校验脚本自身的脆弱点）**：
`og_verify.mjs` §4 有两处**日期硬编码导致的假失败** —— `buildOrderList` 不传 `now` 时默认取 `Date.now()`，
而窗口断言用固定日期 `2026-09-21 15:23` 算，**隔一天就必然报「主窗口 0 条」**（改之前实测就是 0/100）。
修法：提出 `const OG_NOW = new Date(2026, 8, 21, 15, 23, 0)`，两处调用都注入。修完 `FAILURES: 0`。

**校验**：`og_scss.mjs` FAILURES:0（新增 6 条柱体文字断言）、`og_verify.mjs` FAILURES:0、`og_preview.mjs` 正常出图。
浏览器实测计算样式：手动 `rgb(6,95,70)`、自动 `rgb(30,58,138)`、`textShadow: none`，51/100 条柱体带文字。

**新增脚本**：
- `og_ink_check.mjs` —— 文字色候选的对比度体检（纯计算，改配色前先跑它选色）。
- `og_shot_ink.mjs` —— 开静态预览 HTML 截柱体文字（`deviceScaleFactor:2`）。
  ⚠️ 预览页 `.og__plotwrap` 实测只有 ~192px 高（`og_preview.mjs` 的 `DIALOG_VH` 模拟值偏小），
  AUTO 蓝条置底全被裁在视口外，截图前得先把滚动容器撑到 620px。

**产物**：`og-ink-overview.png`（绿条+蓝条同色系深色字同框）、`og-ink-auto.png`（蓝条特写）、`orderGantt-preview.html`。

---

## 新增「执行界面简版」（两个页面，与 execPage 零共享）

**需求**：两个新页面 —— ① 变更计划列表页（element-ui 表格，按系统聚合，最后一列「查看执行」）；
② 执行界面简版（全屏新标签页）。页面二与 execPage 功能相同但布局改为：页头两行小长方形选择器
（第一行选环境、第二行选流）+ 主体左右分栏（左 = 只画选中那一条流、**从上到下**的节点/连线；右 = 文档区占位）。
**硬约束**：除 `mockData/res.js` 外，组件 / 方法 / 接口一律不许共享（用户明确要求低耦合）。

### 落盘位置

| 类型 | 文件 |
| --- | --- |
| 页面一 | `views/tool/execPlanList/index.vue`（新建） |
| 页面二 | `views/tool/execPageSimple/index.vue` + `components/*`（8 个）+ `flowLayout.js` / `nodeKind.js` / `nodeMenu.js` / `nodeParams.js` |
| 接口层 | `api/tool/execPageSimple.js` + 独立开关 `_mockFlagSimple.js` + `_mockApiSimple.js` + `_demo*Simple.js`（7 个） |
| 路由 | `store/modules/permission.js` 的 `STANDALONE_PATHS` 加 `/tool/execPageSimple` |
| 菜单 | `sql/execPageSimple_menu.sql`（两条 insert：1073 列表页 / 1074 执行页隐藏）—— **已于 15:0x 执行进库** |

### 纵向布局（`execPageSimple/flowLayout.js`）

主轴方向 = 执行方向：横向版「层 = 列、组内成员纵向堆叠 + 竖线」，纵向版「层 = 行、组内成员**横向**并排 + **横线**」。
常量改名 `COL_GAP → ROW_GAP`、`GROUP_ROW_GAP → GROUP_COL_GAP`；`links` 由 `{x,y1,y2}` 变 `{y,x1,x2}`；
连线改为「源 unit 外框**底中** → 目标 unit 外框**顶中**」的竖直贝塞尔。

### 页面二要点

- 根 class `.esp`；**模板里不能用 `<label>` / `<aside>`**（`index.scss` 有全局同名选择器），
  左右两栏用 `<section class="esp-flow">` / `<section class="esp-doc">`。
- 两级选择：`envList`（严格按 URL `ids` 顺序，环境名优先接口值、首屏回落 URL `envs`）→ `envFlows` → `currentFlow`（**唯一**算 `layoutFlow` 的地方）。
- `ensureSelection()` 挂在 `fetchAll` 的 `finally` 里，避免轮询换数据后左栏空白。
- URL 契约：`?ids=1,2,3&sys=ACT&envs=[{"planId":..,"env":..}]`，`parseEnvMeta` 解析失败返回 `[]`。
- 页面一 `handleExec` 必须**先同步 `window.open`** 再 `await`（否则被弹窗拦截）；跳转用 `$router.resolve().href`。

### 踩到的两个坑

1. **复制 API 层时动态 import 漏改（真 bug，已修）**：`execPageSimple.js` 的 `mockApi()` 里
   `import("./_mockApi")` 没跟着改成 `./_mockApiSimple` —— 页面看着完全正常，实际整条链路跑的是
   execPage 那套 mock，「零共享」已破但零报错。暴露方式：运行时报错路径出现 `_mockApi.js:143`。
   修完在 `esp_verify.mjs` [C] 段加了防回归断言（抓 `import(...)` 说明符必须 `*Simple`）。
   批量改名后要 `grep -v Simple` 复查，**注释里的旧文件名也要一起改**。
2. **`getPlanExtraInfo` 的成功码是 `0` 不是 `200`**（python `func/*` 那批约定），断言写 200 会假红。

### 校验

新增 `esp_verify.mjs`（Temp 目录，带 `resolve_ext_loader.mjs` 跑），**FAILURES: 0**，共六段：
SFC 编译（10 个）、JS 语法（14 个）、零共享扫描、纵向几何（真实数据 21 条流 / 210 节点 / 53 并行组 / 134 连线）、
与横向版对照、mock 链路 + 路由菜单。详见技能 `ruoyi-ui-node-verify` §13.9 与 §12 脚本表。

**未做**：没有跑构建、没启动前端（按 AGENTS.md，纯前端改动不自动构建）。

### 菜单 SQL 的实际执行（15:05 用户反馈「看不到菜单」后补的）

只生成了 SQL 文件却没执行 —— 菜单走数据库动态路由，不执行就永远看不到入口。用户 15:04 登录了 admin，
那时还没执行，所以他看到的菜单里没有。

- 备份 `sys_menu` → `Temp/sys_menu_backup_20260922.sql`（执行前做的，避免误伤）。
- `mysql --default-character-set=utf8mb4 -D ry-vue < sql/execPageSimple_menu.sql`（**必须指定 utf8mb4**，否则中文菜单名乱码）。
- 执行前先核了两件事：`sys_menu` 的 20 列顺序与 `insert ... values(...)` 一一对应；1073/1074 未被占用。
- `order_num` 用 16/17，正好接在原最大 15（工单甘特图）之后。
- 验证：`select ... where menu_type in ('M','C') and status=0`（即后端 `selectMenuTreeAll` 的等价 SQL）
  确认 1073/1074 都在 admin 的菜单树里。
- **不需要**清 Redis / 重启后端：`SysMenuServiceImpl.selectMenuTreeByUserId` 没有 `@Cacheable`，
  admin（user_id=1）走 `selectMenuTreeAll()` 全量查询，不查 `sys_role_menu`（该表 role_id=1 记录数为 0 即是佐证）。
  前端刷新页面（vuex 重置 → 重新调 `/getRouters`）即可生效。
- 另一个账号 `ry`（user_id=2，普通角色，86 条授权）**看不到**新菜单 —— 若要给它，得往 `sys_role_menu` 补授权。

**教训**：用户说「做个菜单显示出来」时，**SQL 文件 ≠ 菜单已生效**。要么顺手执行，要么明确告诉用户「需要你执行」。
这次是只留了文件、回复里也提了，但用户没注意到 —— 以后这类「需要外部动作才可见」的改动，
做完就该直接问一句或直接执行。

### 主体左右分栏改成 6 : 4（用户要求：流 6 成 / 文档 4 成）

原来是「左栏 `flex:1 1 auto` 弹性 + 右栏 `flex:none; width:360px` 定宽」，改成两栏按比例：

```scss
.esp-flow { flex: 6 1 0; min-width: 0; }   // 流
.esp-doc  { flex: 4 1 0; min-width: 0; }   // 文档
```

**为什么用 `flex: 6 1 0` 而不是 `width: 60% / 40%`**：`.esp-split` 有 14px 的 gap，
百分比写法会变成 `60% + 40% + 14px > 100%` 把容器撑破；basis 归零后剩余空间按 grow 分配，
gap 自动被扣掉，比例才是精确的 6:4。

**顺带删掉**窄屏媒体查询里的 `.esp-doc { width: 260px }` —— `flex-basis:0` 优先于 `width`，
留着不生效还会让人以为还有一层宽度控制。窄屏现在只管 `.esp-progress`（原有行为保留）。

**校验**：`esp_verify.mjs` 新增 [H] 段 —— 先 `sass.renderSync` 编译页面的 scoped style 块，
再从**编译产物**里抠 `.esp-flow` / `.esp-doc` 的规则体断言 flex（源码级 grep 太弱，
后面一条规则就能覆盖前面的）。14 条断言全过，`FAILURES: 0`。dev server 15:10:53 热更新成功。

⚠️ 窄屏（<1280px）下左栏会变窄（原来右栏只吃 260px，现在吃 4 成），流图可能出横向滚动条。
已告知用户，若要窄屏更偏左栏（如 7:3）可以再加。

## 17:2x 工单甘特图：矩形宽度改以 `total_cost` 为准（用户报的 bug）

**用户报的现象**：接口真实返回里 `endTime` 与 `total_cost` 打架，
`CHGU-20260909-0086`（begin 18:00 / total_cost 115 / endTime 23:00）画出来的条明显过长。
要求：**开始时间取 `beginTime`，结束时间看 `total_cost`，不看 `endTime`**。

**根因**：`ganttLayout.js` 的 `resolveSpan()` 原来把顺序写反了 —— 优先 `endTime - beginTime`，
`total_cost` 只在起止无效时兜底。所以样例里 300 分钟的时间差盖过了 115 分钟的耗时。

**改法**（只动一处，判定本来就只在这一处）：
`total_cost > 0` → 用它；否则才回退 `endTime` 差；都没有 → `DEFAULT_DURATION_MIN`。
回退分支刻意保留：`total_cost` 缺失时宁可退回起止时间差，也不要全画成固定 30 分钟。
`fromCost` 语义随之变为「时长不是由起止时间差算出来的」。

| 工单 | beginTime | total_cost | 修复后结束 | 接口 endTime |
| --- | --- | --- | --- | --- |
| CHGU-20260909-0086 | 09-17 18:00 | 115 | **19:55** | 23:00 |
| CHGU-20260909-0087 | 09-17 18:00 | 135 | **20:15** | 22:00 |
| CHGU-20260916-0047 | 09-18 00:00 | 135 | **02:15** | 06:00 |

**连带更新**：`api/tool/orderGantt.js` 的字段说明注释、`ganttLayout.js` 头部三条规则第 1 条、
`og_verify.mjs` §3.2 断言（改为三条：有 cost 忽略 endTime / cost 缺失才回退 / 都缺兜底 30）。

**新增脚本**：`Temp/og_preview_seed.mjs` —— 用**用户这 3 条真实数据**出静态预览 HTML
（`orderGantt-seed-preview.html`），页头带「结束时间核对表」（修复后结束 vs 接口 endTime 划线对比），
以后再有「时间算错」类反馈可直接跑它对照。

**校验**：`og_verify.mjs` FAILURES: 0；`og_diag.mjs` / `og_rows.mjs` 均正常
（demo 造数器本来就是 `endTime = begin + total_cost` 自洽，所以那两套结果不变）。

**记忆整理**：MEMORY.md 已超限（3806 字符），把甘特图全部细节拆到新建的 `REF-order-gantt.md`，
并把告警分析/执行页面/校验三段压缩成指针，MEMORY.md 现 2998 字符。


